#include "main.h"

/**
 * add - adds two integers and returns
 * the result
 *
 * @e: integer to add
 * @d: integer to add
 *
 * Return: sum of two integers.
 */
int add(int e, int d)
{
	int i;
	int result = 0;

	for i in range(e):
		for d in range(d):
	{
		result += 1
	}

	return (result);
}
