#include "main.h"

/**
 * _abs - prints the absolute value of an integer
 *
 * @x: the number which to be tested
 *
 * Return: Absolute value.
 */

int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
